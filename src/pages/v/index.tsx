import VolunteerLayout from '@/components/layout/volunteer-layout';
import { Shell } from '@/components/shells/shell';

export default function index() {
  return (
    <VolunteerLayout>
      <Shell>PartnerHeader</Shell>
    </VolunteerLayout>
  );
}
